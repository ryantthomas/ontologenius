{
  "name": "ontologenius",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ontology-governed knowledge graph tutor — build a graph on any topic via MCP, study it with evidence-based retrieval practice.",
  "license": "Apache-2.0",
  "scripts": {
    "seed": "tsx scripts/seed.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ladybugdb/core": "^0.20.2",
    "ts-fsrs": "^5.2.0",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
